# Terraform Static Website on AWS

This project demonstrates a **Terraform-managed static website** hosted on **AWS S3** with **CloudFront distribution** and **remote Terraform state** stored in S3. It includes modern best practices such as **Origin Access Control (OAC)** and provider version locking.
